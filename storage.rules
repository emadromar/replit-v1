rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public to VIEW images (storefronts need this)
      allow read: if true;
      
      // Only authenticated users can UPLOAD/DELETE
      allow write: if request.auth != null;
    }
  }
}